template <typename T>
class RLE {
    private:
        struct info {
            T kind;
            int size;
        };
    public:
        vector<info> container;
        RLE(vector<T>& input) {
        }
}
